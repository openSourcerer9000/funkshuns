package:
  name: "funkshuns"
  version: "0.3.0"

source:
  # Relative path to the parent directory.
  path: ..

requirements:
  host:
    - python
    - setuptools
  build:
    - python {{ python }}
  run:
    - python
    - pandas
    - requests
    - cython

about:
  home: https://github.com/openSourcerer9000/funkshuns
  license: MIT
  license_familY: MIT
  license_file: LICENSE
  summary: "We got the funk, it's in this repo!"

extra:
  recipe-maintainers:
    - openSourcerer9000